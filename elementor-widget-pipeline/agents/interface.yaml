interface:
  display_name: "Elementor Widget Pipeline"
  short_description: "按确认设计与站点样式契约实现单个 Elementor Widget"
  default_prompt: "Use $elementor-widget-pipeline to implement one Widget from confirmed HTML or an eligible simple reference. Confirm the source mode, site-style contract or explicit local exception, conservative field card, and canonical slug before implementation."

compatibility:
  canonical_format: "agent-skills"
  adapter_targets:
    - openai
    - claude
    - generic
    - vscode
  activation:
    mode: "manual"
    paths: []
  execution:
    context: "inline"
    shell: "bash"
  trust:
    source_tier: "local"
    remote_inline_execution: "forbid"
    remote_metadata_policy: "allow-metadata-only"
  degradation:
    missing_html: "Use the Simple Reference-led Fast Path only when all eligibility rules pass and the user explicitly approves it; otherwise route confirmed design to website-html-prototyper or unresolved design to website-ui-architect."
    missing_plugin: "Stop Widget implementation and route to elementor-site-initialize; resume only after the plugin scaffold and Flat or Grouped structure are confirmed."
